export default ->
  status_ok, neogit = pcall(require, 'neogit')

  unless not status_ok
    neogit.setup {
      commit_editor:
        kind: 'floating'
      
      commit_view:
        kind: 'floating'

      integrations:
        telescope: true
        codediff: true
    }
